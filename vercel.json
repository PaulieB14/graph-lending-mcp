{
  "version": 2,
  "buildCommand": "npm run build && cd dashboard && npm install",
  "outputDirectory": "dashboard/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ],
  "functions": {
    "api/index.js": {
      "memory": 256,
      "maxDuration": 30
    }
  }
}
